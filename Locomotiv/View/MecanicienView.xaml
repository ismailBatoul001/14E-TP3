<UserControl x:Class="Locomotiv.View.MecanicienView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Locomotiv.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="{Binding Titre}"
                   FontSize="20"
                   FontWeight="Bold"
                   Margin="0,0,0,8"/>

        <TextBlock Grid.Row="1"
                   Text="{Binding Message}"
                   Margin="0,0,0,12"
                   Foreground="DarkRed"/>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="0.35*"/>
                <ColumnDefinition Width="0.65*"/>
            </Grid.ColumnDefinitions>

            <GroupBox Header="Trains de ma station" Grid.Column="0" Margin="0,0,12,0">
                <DockPanel>
                    <Button DockPanel.Dock="Top"
                            Content="Rafraîchir"
                            Margin="0,0,0,8"
                            Command="{Binding RefreshTrainsCommand}"/>

                    <ListBox ItemsSource="{Binding Trains}"
                             SelectedItem="{Binding SelectedTrain}"
                             DisplayMemberPath="Numero"
                             Height="260"/>
                </DockPanel>
            </GroupBox>

            <Grid Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <GroupBox Header="Historique des inspections" Grid.Row="0">
                    <DockPanel>
                        <Button DockPanel.Dock="Top"
                                Content="Rafraîchir"
                                Margin="0,0,0,8"
                                Command="{Binding RefreshInspectionsCommand}"/>

                        <DataGrid ItemsSource="{Binding Inspections}"
                                  AutoGenerateColumns="False"
                                  IsReadOnly="True"
                                  CanUserAddRows="False">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Date" Binding="{Binding DateInspection}" Width="*"/>
                                <DataGridTextColumn Header="Type" Binding="{Binding TypeInspection}" Width="*"/>
                                <DataGridTextColumn Header="Résultat" Binding="{Binding Resultat}" Width="*"/>
                                <DataGridTextColumn Header="Observations" Binding="{Binding Observations}" Width="2*"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </DockPanel>
                </GroupBox>

                <GroupBox Header="Créer une inspection" Grid.Row="1" Margin="0,12,0,0">
                    <Grid Margin="10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Grid.Column="0" Text="Type :" VerticalAlignment="Center" Margin="0,0,10,6"/>
                        <ComboBox Grid.Row="0" Grid.Column="1"
                                  ItemsSource="{Binding TypesInspection}"
                                  SelectedItem="{Binding TypeSelectionne}"
                                  Margin="0,0,0,6"/>

                        <TextBlock Grid.Row="1" Grid.Column="0" Text="Résultat :" VerticalAlignment="Center" Margin="0,0,10,6"/>
                        <ComboBox Grid.Row="1" Grid.Column="1"
                                  ItemsSource="{Binding ResultatsInspection}"
                                  SelectedItem="{Binding ResultatSelectionne}"
                                  Margin="0,0,0,6"/>

                        <TextBlock Grid.Row="2" Grid.Column="0" Text="Obs :" VerticalAlignment="Center" Margin="0,0,10,0"/>
                        <TextBox Grid.Row="2" Grid.Column="1"
                                 Text="{Binding Observations, UpdateSourceTrigger=PropertyChanged}"
                                 Height="60"
                                 AcceptsReturn="True"/>

                        <Button Grid.Row="2" Grid.Column="2"
                                Content="Créer"
                                Margin="12,0,0,0"
                                Padding="14,6"
                                VerticalAlignment="Top"
                                Command="{Binding CreerInspectionCommand}"/>
                    </Grid>
                </GroupBox>
            </Grid>
        </Grid>
    </Grid>
</UserControl>
