<UserControl x:Class="Locomotiv.View.MapView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mapsui="clr-namespace:Mapsui.UI.Wpf;assembly=Mapsui.UI.Wpf"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Locomotiv.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <mapsui:MapControl x:Name="mapView" />
        <Border x:Name="StationPopup"
                Background="#F0F0F0"
                BorderBrush="Black"
                BorderThickness="1"
                CornerRadius="8"
                Padding="10"
                Width="350"
                Height="500"
                HorizontalAlignment="Right"
                VerticalAlignment="Top"
                Margin="10"
                Visibility="Collapsed">
            <Grid>
                <Button Content="×" HorizontalAlignment="Right" VerticalAlignment="Top"
                        FontSize="16" FontWeight="Bold" Background="Transparent"
                        BorderThickness="0" Foreground="Black"
                        Click="FermerPopup_Click"/>

                <ContentControl x:Name="PopupContent"
                                Margin="0,30,0,0" />
            </Grid>
        </Border>
    </Grid>
</UserControl>
