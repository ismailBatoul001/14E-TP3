<UserControl x:Class="Locomotiv.View.GestionTrainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Locomotiv.View"
        mc:Ignorable="d">
    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        <Grid Margin="20" Background="White">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" BorderBrush="Gray" BorderThickness="1" Padding="15" Margin="0,0,0,20">
                <StackPanel>
                    <TextBlock Text="Ajouter un train" FontWeight="Bold" FontSize="18" Margin="0,0,0,10"/>

                    <TextBlock Text="Numéro du train:" Margin="0,5,0,2"/>
                    <TextBox Text="{Binding NouveauTrainNumero, UpdateSourceTrigger=PropertyChanged}" Padding="5"/>

                    <TextBlock Text="Capacité:" Margin="0,10,0,2"/>
                    <TextBox Text="{Binding NouveauTrainCapacite, UpdateSourceTrigger=PropertyChanged}" Padding="5"/>

                    <TextBlock Text="Type du train:" Margin="0,10,0,2"/>
                    <ComboBox ItemsSource="{Binding Source={StaticResource TypeTrainValues}}"
                          SelectedItem="{Binding NouveauTrainType}"
                          Padding="5"/>

                    <TextBlock Text="État du train:" Margin="0,10,0,2"/>
                    <ComboBox ItemsSource="{Binding Source={StaticResource EtatTrainValues}}"
                          SelectedItem="{Binding NouveauTrainEtat}"
                          Padding="5"/>

                    <TextBlock Text="Station:" Margin="0,10,0,2"/>
                    <ComboBox ItemsSource="{Binding Stations}"
                          SelectedItem="{Binding SelectedStation}"
                          DisplayMemberPath="Nom"
                          Padding="5"/>

                    <TextBlock Text="{Binding CapaciteStation}"
                           Margin="0,5,0,0"/>

                    <TextBlock Text="{Binding ErrorMessages}"
                           Foreground="Red"
                           Margin="0,10,0,0"
                           TextWrapping="Wrap"/>

                    <Button Content="Ajouter le train"
                        Command="{Binding AjouterTrainCommand}"
                        Margin="0,15,0,0"
                        Padding="10,5"/>
                </StackPanel>
            </Border>

            <Border Grid.Row="1" BorderBrush="Gray" BorderThickness="1" Padding="10">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Text="Liste des trains" FontWeight="Bold" FontSize="16" Margin="0,0,0,10"/>
                    <ListBox Grid.Row="1"
                         ItemsSource="{Binding Trains}"
                         SelectedItem="{Binding SelectedTrain}">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Border BorderBrush="LightGray" BorderThickness="1" Margin="5" Padding="10">
                                    <StackPanel>
                                        <TextBlock Text="{Binding Numero}" FontWeight="Bold" FontSize="14"/>
                                        <TextBlock Text="{Binding Type, StringFormat='Type: {0}'}" Margin="0,5,0,0"/>
                                        <TextBlock Text="{Binding Etat, StringFormat='État: {0}'}"/>
                                        <TextBlock Text="{Binding Capacite, StringFormat='Capacité: {0} passagers'}"/>
                                        <TextBlock Text="{Binding StationActuelle.Nom, StringFormat='Station: {0}'}"/>
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>

                    <Button Grid.Row="2"
                        Content="Supprimer le train sélectionné"
                        Command="{Binding SupprimerTrainCommand}"
                        Margin="0,10,0,0"
                        Padding="10,5"/>
                </Grid>
            </Border>
        </Grid>
    </ScrollViewer>
</UserControl>
